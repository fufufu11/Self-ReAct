# 贡献指南

本项目用于学习和实现最小可用的 ReAct 智能体框架。欢迎提出问题和改进建议，但请保持每次变更范围小且可验证。

## 开始之前

- 先阅读 [项目计划](README.md)，确认当前阶段的目标和不在范围内的内容。
- 不要提交 API Key、`.env` 文件或其他凭据；配置示例应使用 `.env.example`。
- 代码、测试和文档变更应保持在同一个明确的目标内。

## 开发流程

1. 一个对话窗口只处理一个 GitHub Issue。
2. 开始编码前，Issue 必须写清目标、验收标准、范围外内容和验证方式。
3. 从 `main` 创建分支。分支格式使用 `feat/issue-<编号>-<描述>`、`fix/issue-<编号>-<描述>` 或 `chore/issue-<编号>-<描述>`。
4. 每个 PR 默认只关联一个 Issue，并在描述首行添加 `Closes #<issue-number>`。
5. PR 中说明实际执行过的验证命令和结果；不要把未执行的验证写成已通过。
6. 合并前检查改动是否包含密钥、无关格式化或超出 Issue 的功能。

## 提交信息

提交信息采用简洁的 Conventional Commits 风格，例如：

- `feat: 添加工具注册表`
- `fix: 处理无效工具参数`
- `docs: 补充运行示例`
- `test: 覆盖解析失败场景`
- `chore: 配置仓库协作模板`

## 提交问题

请使用仓库提供的 Issue 模板。一个问题只描述一个可以独立验证的目标；复杂工作请拆分为多个相互关联的 Issue。
