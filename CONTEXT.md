# Self-ReAct 领域上下文

本上下文统一描述 Self-ReAct 的最小 ReAct 运行语言，帮助模型、工具、循环控制器和执行轨迹使用同一组概念。它只记录领域定义、边界和关系，不记录 Python 字段或模块实现细节。

## 交互对象

**Message**：进入模型上下文的一条对话消息，由 system、user、assistant 或 tool 角色产生。它表达上下文内容，不等同于工具动作。
_避免_：把所有消息都称为 Observation 或把助手的工具请求直接称为工具结果。

**ToolCall**：模型请求执行的一次具体工具动作，包含可关联本次调用的编号、精确工具名称和参数。它表达意图，不代表工具已经执行成功。
_避免_：Action、ToolRequest（在本项目中统一使用 ToolCall）。

**ToolResult**：一次 ToolCall 执行后的结构化成功或失败结果。成功和失败互斥；失败必须保留稳定错误类别、面向模型的说明和可恢复性。
_避免_：把异常字符串放进成功内容，或把 ToolResult 与 Observation 当作同义词。

**Observation**：由 ToolResult 写回模型上下文的可读观察。它保留工具调用关联以及失败状态，服务于下一轮决策，不代替原始 ToolResult。
_避免_：用 Observation 表示任意调试日志。

## 运行状态

**AgentState**：一次 ReAct 运行在循环之间需要保留的任务状态，包括消息上下文、可用工具名称、步数预算、执行轨迹和终止信息。它不包含模型客户端、Python 函数、密钥或其他运行时资源。
_避免_：把 AgentState 当作 Agent 主循环或模型客户端。

**TraceStep**：一次决策尝试的执行轨迹记录，描述输入摘要、工具决策或最终回答、观察、错误和耗时。它只保留可序列化且有助于解释执行的摘要，不默认保存完整隐藏推理。
_避免_：把 TraceStep 当作完整对话消息或持久化数据库记录。

**FinalAnswer**：模型决定结束当前运行时交付给调用方的回答。它是决策的一种，与请求工具的 ToolCall 互斥。
_避免_：把任何 assistant Message 都当作最终回答。

**Plan / Reflection**（R-06 可选模式）：规划阶段的执行计划与反思阶段的失败原因总结。二者都是决策的一种（与 FinalAnswer 同构，只含非空文本），仅在开启 `plan_mode` / `reflection_mode` 时出现，分别记录在 TraceStep 中供解释性展示。
_避免_：把 Plan / Reflection 当作工具调用或最终回答，它们不参与工具分派。

**TerminationReason**：循环停止时对外报告的最终原因，例如最终回答、步数耗尽、模型输出解析失败、未知工具或工具执行失败。可恢复的工具错误可以先作为 Observation 返回模型，只有控制器决定停止时才成为终止原因。
_避免_：把每一次工具失败都直接当作不可恢复的终止。
